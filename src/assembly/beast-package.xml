<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>beast-package</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <!-- version.xml from project root -->
    <files>
        <file>
            <source>${project.basedir}/version.xml</source>
            <outputDirectory>/</outputDirectory>
        </file>
    </files>

    <!-- Module JAR only (BEAST core, JavaFX, and their transitive deps are provided by the runtime) -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>/lib</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <scope>runtime</scope>
            <includes>
                <include>io.github.compevol:morph-models</include>
            </includes>
        </dependencySet>
    </dependencySets>

    <fileSets>
        <!-- fxtemplates/ -->
        <fileSet>
            <directory>${project.basedir}/src/main/resources/morph.models/fxtemplates</directory>
            <outputDirectory>/fxtemplates</outputDirectory>
        </fileSet>
        <!-- examples/ -->
        <fileSet>
            <directory>${project.basedir}/examples</directory>
            <outputDirectory>/examples</outputDirectory>
            <excludes>
                <exclude>legacy*/**</exclude>
            </excludes>
        </fileSet>
    </fileSets>
</assembly>
